# laravel-matrix-project

Version 1.0

-------- modified files directory --------

Migration files => database -> migrations

Models => app -> Models

Controllers => app -> HTTP => Controllers -> Api

Special Requests => app -> HTTP => Requests
